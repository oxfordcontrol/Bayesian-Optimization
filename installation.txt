# Anaconda 5.0.1
# It is recommended to install on a new environment

conda install numpy scipy matplotlib pyyaml r
pip install GPy==1.8.4 numdifftools
pip install --upgrade tensorflow=1.3.0

pip install 'scs>=2.0.2' --no-binary :all:
pip install 'cvxpy<1' --no-binary :all:
conda install -c mosek mosek=8.1.33

# Library for LP-EI
# Install the following MODIFIED version of GPyOpt
# https://github.com/nrontsis/GPyOpt/tree/697b950

# Install MODIFIED DiceOptim version
# https://github.com/nrontsis/DiceOptim/tree/643c7f6
# (Run: install.packages('devtools') library('devtools') build(./)
# and install(./) from R, when inside DiceOptim's folder)
# You might need to install compilers in order to install DiceOptim
# https://conda.io/docs/user-guide/tasks/build-packages/compiler-tools.html

# gpflow
https://github.com/GPflow/GPflow/releases/tag/0.5.0